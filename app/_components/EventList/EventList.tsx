// TODO: Implement event list component
// Should support both grid and list layouts
// Display: name, date, venue(s), sport type
// Clickable to navigate to edit/view

export default function EventList() {
  return (
    <div>
      {/* TODO: Map through events and render EventCard components */}
    </div>
  );
}

